<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="action_messenger_jobs" model="ir.actions.act_window">
        <field name="name">Messenger Jobs</field>
        <field name="res_model">messenger.job</field>
        <field name="view_mode">tree,form</field>
        <field name="view_id" ref="view_messenger_job_tree"/>
        <field name="help" type="html">
            <p class="oe_view_nocontent_create">
                Create the first Messenger Job
            </p>
        </field>
    </record>

    <menuitem
        id="messenger_jobs_menu"
        name="Messenger Jobs"
        action="action_messenger_jobs"
        parent="account.menu_finance_receivables"
        sequence="15"/>

</odoo>
