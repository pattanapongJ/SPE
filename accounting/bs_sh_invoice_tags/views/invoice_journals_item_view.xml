<?xml version="1.0"?>
<odoo>
    <!-- General Items view -->
    <record id="view_move_line_tree_grouped_inherit" model="ir.ui.view">
        <field name="name">view_move_line_tree_grouped_inherit</field>
        <field name="model">account.move.line</field>
        <field name="inherit_id" ref="account.view_move_line_tree_grouped"></field>
        <field name="arch" type="xml">
            <field name="partner_id" position="after">
                <field name="invoice_tag_ids" widget="many2many_tags" options="{'color_field': 'color', 'no_create_edit': True}" optional="show"/>
            </field>
        </field>
    </record>

</odoo>
