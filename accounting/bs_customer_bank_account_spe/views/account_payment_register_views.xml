<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- View account.payment.register form -->
    <record id="view_account_payment_register_form" model="ir.ui.view">
        <field name="name">view.account.payment.register.form</field>
        <field name="model">account.payment.register</field>
        <field name="inherit_id" ref="account.view_account_payment_register_form"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='partner_bank_id']" position="after">
                <field name="customer_bank_id" domain="[('partner_id','=',partner_id)]" attrs="{'invisible':['|',('payment_type','!=','inbound'),('partner_type','!=','customer')]}"
                       options="{'no_create_edit': True,'no_create': True, 'no_quick_create': True}"/>

            </xpath>
        </field>
    </record>

</odoo>