<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- View cheque.bounce.reason View Tree -->
    <record id="view_cheque_bounce_reason_tree" model="ir.ui.view">
        <field name="name">view.cheque.bounce.reason.tree</field>
        <field name="model">cheque.bounce.reason</field>
        <field name="arch" type="xml">
            <tree editable="bottom">
                <field name="code"/>
                <field name="name"/>
                <field name="description"/>
            </tree>
        </field>
    </record>



    <!-- Action cheque.bounce.reason -->
    <record id="action_cheque_bounce_reason" model="ir.actions.act_window">
        <field name="name">Cheque Bounce Reason</field>
        <field name="res_model">cheque.bounce.reason</field>
        <field name="view_mode">tree</field>
        <field name="domain">[]</field>
        <field name="context">{}</field>
        <field name="help" type="html">
            <p class="oe_view_nocontent_create">
                There is no examples click here to add new Cheque Bounce Reason.
            </p>
        </field>
    </record>

    <menuitem id="menu_cheque_bounce_reason" name="Cheque Bounce Reason" parent='account.menu_finance_configuration' action="action_cheque_bounce_reason" sequence="1"/>


</odoo>
