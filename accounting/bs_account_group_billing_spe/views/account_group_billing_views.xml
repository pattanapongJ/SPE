<odoo>
    <record id="view_account_group_billing_tree" model="ir.ui.view">
        <field name="name">account.group.billing.tree</field>
        <field name="model">account.group.billing</field>
        <field name="arch" type="xml">
            <tree
                decoration-info="state == 'draft'"
                decoration-muted="state == 'cancel'"
                edit="false"
            >
                <field name="name" />
                <field name="partner_id" string="Customer" />
                <field name="branch" />
                <field
                    name="state"
                    widget="badge"
                    decoration-success="state == 'billed'"
                    decoration-info="state == 'draft'"
                    decoration-danger="state == 'cancel'"
                />
            </tree>
        </field>
    </record>
    <record id="view_group_billing_search" model="ir.ui.view">
        <field name="name">account.group.billing.search</field>
        <field name="model">account.group.billing</field>
        <field name="arch" type="xml">
            <search string="Search Group Billings">
                <field name="name" string="Name" />
                <field name="partner_id" />
                <filter
                    name="state_draft"
                    string="Draft"
                    domain="[('state','=','draft')]"
                    help="Draft Billing"
                />
                <filter
                    name="state_billed"
                    string="Billed"
                    domain="[('state','=','billed')]"
                    help="Billed Billing"
                />
                <separator />
                <group expand="0" string="Group By">
                    <filter
                        name="group_by_customer"
                        string="Customer"
                        context="{'group_by':'partner_id'}"
                    />
                    <filter
                        name="group_by_state"
                        string="Status"
                        context="{'group_by':'state'}"
                    />
                </group>
            </search>
        </field>
    </record>
    <record id="view_account_group_billing_form" model="ir.ui.view">
        <field name="name">account.group.billing.form</field>
        <field name="model">account.group.billing</field>
        <field name="arch" type="xml">
            <form>
                <header>
                    <button
                        name="validate_billing"
                        class="oe_highlight"
                        states="draft"
                        string="Validate"
                        type="object"
                    />
                    <button
                        name="action_cancel"
                        states="draft"
                        string="Cancel"
                        type="object"
                    />
                    <button
                        name="action_cancel"
                        states="billed"
                        string="Cancel"
                        type="object"
                        confirm="Are you sure to cancel this document?"
                    />
                    <button
                        name="action_cancel_draft"
                        states="cancel"
                        string="Set to Draft"
                        type="object"
                    />
                    <field
                        name="state"
                        widget="statusbar"
                        statusbar_visible="draft,billed"
                    />
                </header>
                <sheet>
                    <div class="oe_button_box" name="button_box">
                        <button
                            class="oe_stat_button"
                            name="invoice_relate_billing_tree_view"
                            type="object"
                            attrs="{'invisible':[('state', 'not in', ['billed'])]}"
                            icon="fa-pencil-square-o"
                        >
                            <field
                                name="invoice_related_count"
                                string="Invoices"
                                widget="statinfo"
                            />
                        </button>
                    </div>
                    <h1>
                        <label
                            for="name"
                            string="Draft Billing"
                            attrs="{'invisible': [('state','!=', 'draft')]}"
                        />
                        <field
                            name="name"
                            readonly="1"
                            class="oe_inline"
                            attrs="{'invisible': [('state', '=', 'draft')]}"
                        />
                    </h1>
                    <group>
                        <group>
                            <field
                                name="partner_id"
                                attrs="{'readonly': [('state', '!=', 'draft')]}"
                            />
                            <field name="company_id" invisible="1" />
                        </group>
                        <group>
                            <field name="date" />
                            <field name="branch" />
                        </group>
                    </group>
                    <notebook>
                        <page string="Billing Lines" groups="base.group_user">
                            <field name="billing_line_ids">
                                <tree name="billing" create="1" editable="bottom">
                                    <field name="invoice_id" />
                                    <field name="name" readonly="1" force_save="1" />
                                    <field
                                        name="invoice_date"
                                        readonly="1"
                                        force_save="1"
                                    />
                                    <field
                                        name="threshold_date"
                                        readonly="1"
                                        force_save="1"
                                    />
                                    <field name="origin" readonly="1" force_save="1" />
                                    <field
                                        name="total"
                                        sum="Total Amount"
                                        readonly="1"
                                        force_save="1"
                                    />
                                    <field
                                        name="state"
                                        readonly="1"
                                        force_save="1"
                                        widget="badge"
                                        decoration-success="state == 'posted'"
                                        decoration-info="state == 'draft'"
                                        decoration-danger="state == 'cancel'"
                                    />
                                    <field
                                        name="payment_state"
                                        widget="badge"
                                        decoration-success="payment_state == 'paid'"
                                        decoration-info="payment_state == 'not_paid'"
                                        decoration-danger="payment_state == 'reversed'"
                                        decoration-warning="payment_state == 'in_payment'"
                                    />
                                </tree>
                            </field>
                            <group>
                                <group>
                                    <separator string="Notes" />
                                    <field name="narration" colspan="4" nolabel="1" />
                                </group>
                                <group />
                            </group>
                        </page>
                    </notebook>
                </sheet>
                <div class="oe_chatter">
                    <field name="message_follower_ids" widget="mail_followers" />
                    <field name="message_ids" widget="mail_thread" />
                </div>
            </form>
        </field>
    </record>
    <record id="action_customer_group_billing" model="ir.actions.act_window">
        <field name="name">Group Billing</field>
        <field name="res_model">account.group.billing</field>
        <field name="view_id" eval="view_account_group_billing_tree" />
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
            Click to register a new group billing.
          </p>
            <p>
            Enter the customer the invoices expect to be paid by the customer for this
            bill period. By default the pending invoices will be listed, automatically.
          </p>
        </field>
    </record>

    <menuitem
        action="action_customer_group_billing"
        id="menu_action_customer_group_billing"
        name="Group Billing"
        parent="account.menu_finance_entries_management"
        sequence="21"
    />
</odoo>
