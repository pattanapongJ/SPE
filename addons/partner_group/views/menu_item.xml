<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- actions -->
    <record id="action_contacts_partner_group" model="ir.actions.act_window">
        <field name="name">Partner Group</field>
        <field name="res_model">partner.group</field>
        <field name="view_mode">tree,form</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
            Create new Partner Group
            </p>
        </field>
    </record>

    <!-- menu item -->
    <menuitem
        id="menu_contacts_company_type"
        name="Company Type"
        parent="contacts.res_partner_menu_config"
        groups="base.group_system,sales_team.group_sale_manager"
        sequence="30"/>
    <menuitem
        id="menu_contact_partner_group"
        name="Partner Group"
        parent="menu_contacts_company_type"
        sequence="1"
        action="action_contacts_partner_group"
    />

</odoo>
