<odoo>
    <record id="purchase_order_form_add_field" model="ir.ui.view">
        <field name="name">purchase.order.form.add.fields</field>
        <field name="model">purchase.order</field>
        <field name="inherit_id" ref="purchase.purchase_order_form"/>
        <field name="arch" type="xml">
            <field name="product_qty" position="after">
                <field name="finished_qty" optional="hide"/>
                <field name="finished_note" optional="hide"/>
            </field>

            <field name="incoterm_id" position="after">
                <field name="carrier_id"/>
            </field>

            <!-- <xpath
                expr="//field[@name='order_line']/tree//field[@name='secondary_uom_qty']"
                position="attributes"
            >
                <attribute name="optional">hide</attribute>
                
            </xpath>

            <xpath
                expr="//field[@name='order_line']/tree//field[@name='secondary_uom_id']"
                position="attributes"
            >
                <attribute name="optional">hide</attribute>
                
            </xpath> -->

        </field>
    </record>
</odoo>