<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="wizard_tms_confirm_trl_view_form" model="ir.ui.view">
        <field name="name">wizard.tms.confirm.trl.view.form</field>
        <field name="model">wizard.tms.trl</field>
        <field name="arch" type="xml">
            <form string="รายงานการจัดส่งสินค้าตามผู้จัดส่ง-แบบสรุป (รายวัน) สายส่ง TRL">
                <sheet>
                    <group>
                        <label for="from_date" />
                        <div class="o_row">
                            <field name="from_date" />
                            <p class="font-weight-bold">To</p>
                            <field name="to_date" />
                        </div> 
                        <field name="transport_desc_ids" widget="many2many_tags" options="{'no_create': True, 'no_open': True}"/>
                        <field name="delivery_status" widget="many2many_tags" domain="[('name', '!=', 'False')]" options="{'no_create': True, 'no_open': True}"/>
                        <field name="billing_status" widget="many2many_tags" domain="[('name', '!=', 'False')]" options="{'no_create': True, 'no_open': True}"/>
                        <field name="driver_ids" widget="many2many_tags" options="{'no_create': True, 'no_open': True}"/>
                        <field name="tms_remark" />
                    </group>
                    <footer>
                        <button name="print_pdf" string="Print PDF" type="object"
                            class="btn-primary" />
                        <button name="print_excel" string="Print Excel" type="object"
                            class="btn-primary" />
                    </footer>
                </sheet>
            </form>
        </field>
    </record>

    <record id="wizard_tms_confirm_trl_action" model="ir.actions.act_window">
        <field name="name">รายงานการจัดส่งสินค้าตามผู้จัดส่ง-แบบสรุป (รายวัน) สายส่ง TRL</field>
        <field name="res_model">wizard.tms.trl</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
    </record>

    <menuitem id="menu_tms_confirm_trl_report" name="รายงานการจัดส่งสินค้าตามผู้จัดส่ง-แบบสรุป (รายวัน) สายส่ง TRL" parent="hdc_tms.menu_tms_reporting_main"
        action="wizard_tms_confirm_trl_action" sequence="5" />

</odoo>