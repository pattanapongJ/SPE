<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <record id="view_picking_list_backorder_form" model="ir.ui.view">
            <field name="name">Create Backorder</field>
            <field name="model">wizard.picking.list.backorder</field>
            <field name="arch" type="xml">
                <form string="Create a Backorder">
                    <group>
                        <p>
                            You have processed less products than the initial demand.
                        </p><p class="text-muted">
                            Create a backorder if you expect to process the remaining
                            products later. Do not create a backorder if you will not
                            process the remaining products.
                        </p>
                    </group>
                    <field name="picking_lists" invisible="1"/>
                    <footer>
                        <button name="action_backorder" string="Create backorder" type="object" class="btn-primary"/>
                        <button name="action_close" type="object" string="No Backorder"/>
                        <button string="Discard" class="btn-secondary" special="cancel" />
                    </footer>
                </form>
            </field>
        </record>

        <record id="action_picking_list_backorder" model="ir.actions.act_window">
            <field name="name">You produced less than initial demand</field>
            <field name="type">ir.actions.act_window</field>
            <field name="res_model">wizard.picking.list.backorder</field>
            <field name="view_mode">form</field>
            <field name="target">new</field>
        </record>
       
    </data>
</odoo>    
