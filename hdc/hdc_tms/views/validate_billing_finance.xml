<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="action_validate_billing_finance" model="ir.actions.server">
        <field name="name">Validate Billing/Finance</field>
        <field
            name="model_id"
            ref="model_account_move"
        />
        <field name="binding_model_id" ref="model_account_move" />
        <field name="binding_view_types">list</field>
        <field name="state">code</field>
        <field name="code">records.validate_billing_finance()</field>
    </record>

</odoo>